<div class="col col-9">
  <h3>
    All Tags
  </h3>
  <ul class="p-0">
    <% @tags.each do |t| %>
      <li>
        <a href="<%= base_url %>/tag/<%= t.id %>">
          <%= t.name %>
          |
          <%= t.sources.count %>
        </a>
      </li>
    <% end %>
  </ul>
</div>
