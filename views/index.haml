.col.col-9
  %h3
    My Feed

  %ul
  - @articles.each do |a|
    %li
      %span
        %image.favicon{src: "#{a.source.favicon}"}
      -# = a.source.name
      = a.published_at.strftime('%m/%d/%Y') if a.published_at
      |
      %a{href: "#","data-title": a.title, "data-body": a.body, "data-url" => a.url, "data-target" => "#articleModalLong", "data-toggle" => "modal"}

        = a.title
